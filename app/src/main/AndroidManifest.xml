<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cose.easywu">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".base.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".app.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".app.LoginActivity" />
        <activity android:name=".app.MainActivity" />
        <activity android:name=".app.RegistActivity" />
        <activity android:name=".app.FindPwdActivity" />
        <activity android:name=".app.CheckVerifyCodeActivity" />
        <activity android:name=".app.ResetPwdActivity" />
        <activity android:name=".user.activity.EditUserInfoActivity" />
        <activity android:name=".user.activity.EditPwdActivity" />
        <activity android:name=".user.activity.EditNickActivity" />
        <activity
            android:name=".user.activity.EditPhotoActivity"
            android:theme="@style/EditPhotoDialogStyle" />

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.cose.easywu.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity android:name=".release.activity.ChooseTypeActivity"></activity>
    </application>

</manifest>